Fase 2: Estruturação de Dados e IA (Bônus)Crie funções para exportar os dados coletados para os formatos .json e .csv.  Crie uma função que receba a descrição longa do livro e faça uma chamada a um LLM (pode mockar a chamada da API se necessário) para extrair uma informação estruturada, como "Tom do livro" ou "Categorias inferidas".  Adicione testes unitários usando Jest para validar o parsing dos preços, a conversão das estrelas para números e a exportação de arquivos. O comando deve ser npm run test.  

Fase 3: Dockerização e PersistênciaCrie um Dockerfile usando multi-stage build. Use a imagem base node:alpine ou slim. Configure para rodar com um usuário não-root e exponha uma porta. Adicione comentários explicando cada etapa. Crie um arquivo docker-compose.yml que levante o scraper e um banco de dados PostgreSQL para persistência, cobrindo o requisito de bônus. 

Fase 4: Pipeline CI/CD (GitLab)Crie o arquivo .gitlab-ci.yml com as seguintes stages e regras: lint: Roda o ESLint, falha se houver erros. test: Roda o Jest, falha se houver erros. build: Constrói a imagem Docker e faz o push para o GitLab Container Registry usando variáveis nativas do CI. deploy: Simula o deploy (comando echo) rodando apenas na branch main. Bônus: Adicione configuração de cache no pipeline (ex: na pasta node_modules) para acelerar os builds. 

Fase 5: Documentação (README.md)Gere um template de README.md bem estruturado contendo: Passo a passo de como rodar localmente com e sem Docker. O schema de dados do JSON/CSV resultante. Explicação do pipeline CI/CD. Justificativa das decisões técnicas (ex: Playwright para já cobrir o bônus de páginas dinâmicas). Uma seção de "Próximos Passos" detalhando observabilidade e defesas anti-bot. Um espaço reservado para eu preencher com os prompts que utilizei nesta I
